name: Smoke

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/smoke.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "hello"
